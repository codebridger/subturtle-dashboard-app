name: Deploy dev

# Every push to dev goes to the subturtle-dev Firebase project (see deploy.yml).

on:
  push:
    branches: [dev]
  workflow_dispatch:

jobs:
  deploy:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/deploy.yml
    with:
      environment: dev
